// question:  find the ouput of following code (based on increment (pre increment an d post increment of user defined input))
#include<stdio.h>
int main( )
{
	int a=1,b=1,c;
	c=a++ +b;
	printf("\n%d %d %d",a,b,c);
	return 0;
}
