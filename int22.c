// return  statemnt  : exits the function.
//  return exits immedaitely from the currently executing function to the calling routine, optionally returning a value.
// syntax:  return[expression];
// question  :  return ing the value pf square of user defined function
# include<stdio.h>  
int main()
{
	int x;
	printf("enter a number : ");
	scanf("%d",&x);
	printf(int sqr (int x);
{
		return (x*x);
	}
	return 0;
}
